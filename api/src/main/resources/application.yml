spring:
  application:
    name: spring-aws-rest-api
server:
  servlet:
    context-path: /api
  port: ${PORT:8080}
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /docs
  show-actuator: true

security:
  jwt:
    secret-key: ${JWT_SECRET_KEY:afb789a987d29a72f8563162167be815}
    expiration: ${JWT_EXPIRATION:3600000}